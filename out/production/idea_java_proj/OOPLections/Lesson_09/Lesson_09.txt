1. Если мы делаем метод абстрактным,
то логику его реализации можно написать сразу при вызове экземпляра класса
2. Итератор и его методы
имплементируем итератор к классу и пользуемся
3. Компаратор
иплементируем его к классу и сравниваем объекты
есть еще сравнение объектов через compare
return Integer.compare(this.age,o.age);
print.err?
db sort(w1,w2)?